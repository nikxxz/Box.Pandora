export { Colors } from './colors';
export { DIMENSIONS } from './dimensions';
export {
  ANDROID_API,
  ANDROID_PERMISSIONS,
  IS_ANDROID,
  ANDROID_SDK,
  PERMISSION_RATIONALE,
} from './permissions';
export { APP_VERSION, APP_VERSION_SHORT, APP_BUILD } from './version';

// Theme system — preferred for new code
export {
  Theme,
  Colors as ThemeColors,
  Spacing,
  Typography,
  Radii,
  Icons,
} from '../theme';
